target: par2_protect
mime_types: ["*/*"]
description: "Creates SHA-256 checksums and PAR2 recovery volumes to protect against bitrot."

actions:
  - type: checksum
    algorithm: "sha256"
    destination: "{output.dir}/{file.name}.sha256"
  
  - type: par2
    args: "create -r10 '{output.dir}/{file.name}.par2' '{file.path}'"
